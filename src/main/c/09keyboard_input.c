// #define _CRT_SECURE_NO_WARNINGS
// If you use the method above to disable the warnings, it must be put at the top of the file
#include <stdio.h>
#pragma warning(disable:4996)

int main09() {
    int a;
    scanf("%d", &a);
    printf("Result: %d\n", a);
    return 0;
}